@model IEnumerable<Page>

@foreach (var item in Model)
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Pages" asp-action="Index" asp-route-slug="@item.Slug">@item.Title</a>
    </li>
}